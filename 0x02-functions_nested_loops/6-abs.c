#include "main.h"
/**
 * _abs - prints the absolute value of an integer
 * @x: the number
 * Return: 0 success *
 */
int _abs(int x)
{
	if (x < 0)
	{
		_putchar('x');
	}
	else
	{
		_putchar('x');
	}
}
